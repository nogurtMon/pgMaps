services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DSN_ENCRYPTION_KEY=${DSN_ENCRYPTION_KEY}
      - SHARES_DIR=/app/data/shares
    volumes:
      - ./data/shares:/app/data/shares
    restart: unless-stopped
