# v0.1.0 Feature Roadmap

## Tile Performance
- [x] **Tile cache invalidation** — `v=dataVersion` busts cache on control changes; `invalidateTilesByPrefix` ready for data-edit routes when feature editing is built
- [x] **Simplify geometries at lower levels** — speeds up tiling performance with no real downside.

## Data & Editing
- [x] **Refactor layer controls and legend**
- [x] **Feature attribute editing (update/delete) via map** —  click a feature to edit its attributes and save back to PostGIS
- [x] **Feature geometry editing  via map** —  points, lines and polygons
- [x] **Draw & insert** — draw points/lines/polygons directly on the map and insert them into PostGIS
- [x] **Feature editing (update/delete ) via attribute table**
- [x] **Commit changes** - requires users to commit changes to save to their database.

## Sharing & Collaboration
- [x] **Password-protected share links** — optional password on `/share/[id]`
- [x] **Embed code** — one-click `<iframe>` snippet for share links

## UI & UX
- [x] **Multi-layer attribute table** — done (`AttributeTablePanel` with layer switcher)
- [x] **Map navigation controls** — zoom, reset north on main map
- [x] **Copy attribute value** — copy button in feature detail popup
- [x] **Build out /maps to manage maps** — copy button in feature detail popup

## Basemaps
- [x] **OpenFreeMap by default** — without config or env variables, OpenFreeMap should be the default
- [x] **Support basemap connections** — allow user to manage basemap connections, like from MapTiler and Mapbox

## Quality Control
- [x] **Develop QUALITY-CONTROL.md**
- [] **Perform quality control**

## README.md and Demo
- [] **Update README.md** — only support Docker deployment. pre-build docker containers
- [] **Record new demo** — shorter, no audio